apiVersion: v1
kind: Pod
metadata:
  name: pod
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: 256Mi
        cpu: 0.5
      limits:
        memory: 1Gi
        cpu: 2